[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
markers =
    playwright: browser-driven workbench tests (tests/e2e; needs pytest-playwright)
    e2e: requires local servers at 8990 (shell) and 8899 (API)

# OpenUser UX regression — DISABLED (local exploratory use only).
# Critical flows are covered by tests/playwright/ in CI (.github/workflows/ci.yml).
#
# To run OpenUser locally:
#   ./scripts/openuser run --headless --spec 'openuser/specs/*.ux' --project prj_YhR-5cBgb_7E
#   ASSURE_BASE_URL=https://staging.getassureai.com  # optional override

name: UX Regression (OpenUser)

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  disabled:
    if: false
    runs-on: self-hosted
    timeout-minutes: 15
    steps:
      - run: echo "OpenUser is not run in CI. Use tests/playwright/ or run ./scripts/openuser locally."

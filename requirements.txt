jinja2>=3.1.4
pydantic>=2.7.0
instructor>=1.3.0
litellm>=1.50.0
rich>=13.9.0
flask>=3.0.0
flask-limiter>=3.8.0
python-dotenv>=1.0.0
tiktoken>=0.7.0
flask-httpauth>=4.8.0
flask-babel>=4.0.0
cryptography>=43.0.0
z3-solver==5.1.0
dspy-ai>=2.4.0
boto3>=1.34.0
python-docx>=1.1.2
httpx>=0.27.0
asgiref>=3.7.0
psutil>=5.9.0
pypdf>=4.0.0
pyyaml>=6.0.0
flask-wtf>=1.2.0
bleach>=6.1.0
sentry-sdk[flask]>=2.0.0
pytest-asyncio>=0.23.0
gevent>=24.2.0
celery[sqs,redis]>=5.4.0
sqlalchemy>=2.0.0
# PostgreSQL backend (DATABASE_URL=postgresql://...). See db/pg_compat.py.
psycopg[binary,pool]>=3.2
# Redis: Celery broker/results, rate-limit counters, debounce locks (REDIS_URL).
redis>=5.0
resend>=2.0.0
playwright>=1.49.0
# PDF parse paths. Each is required by a distinct endpoint:
#   pymupdf  -> services/pdf_import.pdf_bytes_to_jdf   (POST /api/projects/<id>/import-pdf)
#   jdf CLI  -> services/jdf_converter                 (POST /api/projects/<id>/jdf/ingest)
# Textract is the substrate fallback and needs AWS credentials, not a package.
# Docling is NOT here. It is ML inference and pulls a CUDA toolkit unusable on
# this ARM64 target, and parsing is synchronous on staging today, so there is no
# worker to host it. Deferred — see docs/deferred-parse-runtime.md, which also
# records the worker split (requirements-worker.txt) that replaces this note.
pymupdf>=1.24.0
Pillow>=10.0

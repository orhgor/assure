# Anti-Claims

```
| Claim | Why it's false/unverified | Correct phrasing |
|-------|--------------------------|------------------|
| "OMP provides semantic search" | v0.2.0 is keyword-only; omp_recall sends mode:"keyword" (omp_client.py:146) | "OMP provides keyword search for v1; semantic embed deferred." |
| "Path A surgical edit passes" | structurally true, outcome broken — live model returns DeepSeek AuthenticationError and the node renders an error string (runtime E2E, pathA2.log). UNVERIFIED 2026-09-18: re-checking needs a live provider call, which was out of scope for this pass | "The Path A rephrase flow opens/submits/closes, but model output is currently a DeepSeek auth error, so content is unverified." |
| "Free-model stack is stable" | DeepSeek auth is currently failing (runtime E2E). UNVERIFIED 2026-09-18: depends on live provider auth, not re-measured | "The free-model stack is unstable until DeepSeek auth resolves." |
| "Deploy reports success = app serves" | silent-success bug — a deploy was green while /workbench still 404 (image pulled but not started); state.md Known debt #7. UNVERIFIED 2026-09-18: the deploy path was not exercised in this pass | "Deploy success means the run finished; it does not verify the new code is actually serving." |
| "Two JDF formats are compatible" | different schemas: PyMuPDF → jdf_documents via save_jdf_revision vs jdf-cli → jdf_cli_documents ({$jdf,meta,pages}); state.md debt #1. Re-confirmed 2026-09-18: both tables still coexist hold separate rows (81 vs 50 on staging) | "Two JDF schemas coexist and are not interchangeable." |
| "Regulatory erasure is implemented" | only user-level /api/account/delete (web.py:900); no doc-level delete/forget exists (grep of routers/services empty, re-checked 2026-09-18) | "User-level delete exists; document-level erasure is not implemented." |
| "Auth is enforced on staging" | was false until 2026-09-18 (state.md I1: the boundary was a single env var). Now true for signed-in users, but **not** by `ASSURE_ENFORCE_OWNERSHIP` — that variable is still unset, and this note must not imply the pattern is what binds. What binds is a Clerk identity: `middleware.ownership_enforced()` falls back to `auth_required() and current_user_id()`, so `check_project_ownership` runs under a signed-in user, admins bypass it, and a NULL-owner row stays open. Measured live 2026-09-18T20:48Z: non-admin session sees its own plus unowned projects (9 at that moment), an admin session sees all (136), another owner's JDF and export both return 403, and `/api/analytics/*` is 403 for a non-admin / 200 for an admin. Counts move as projects are created — treat them as measured-at, never as standing facts | "Auth was not enforced until 2026-09-18. Now: Clerk sessions, per-project ownership, role-gated admin." |
| "The workbench is closed to the internet" | the edge is still gated by the **shared** `SHELL_ACCESS_KEY`, not by Clerk. Measured 2026-09-18T20:48Z: an unauthenticated request gets 302 to /auth, but a caller holding only that key and **no Clerk session** gets 200 on `/` and 200 on `/api/projects` (136 projects). A bearer token on `/api` is a programmatic path that carries no user identity and returns the unrestricted view — it is not a session substitute | "Until the shared key is retired, one shared secret closes the shell; Clerk adds per-user identity inside it and does not by itself close /api." |
| "Every protected page needs a login" | `/` appears in both `PROTECTED_HTML` and `PUBLIC_HTML` in cloud_auth.py, and the public list is consulted first, so Flask serves `/` with no session. Measured 2026-09-18T20:48Z on the Flask origin: `/` → 200, while `/compose`, `/account` and `/analytics` → 302 to /signin | "Compose, account and analytics redirect to /signin; the Flask root is exempt by an ordering quirk in PUBLIC_HTML." |
| "Verification is surfaced end-to-end for the client" | full_context_scan / macro_verify are backend-only; no refs in prototype/shell.js (grep empty, re-checked 2026-09-18 after this wave's shell edits: 0 matches) | "Verification engines exist in the backend but are not surfaced in the prototype." |
| "The knowledge vault works from the prototype" | vault_tfidf_cache / fast_router are not wired into the prototype UI; the prototype exposes only keyword JDF search (re-checked 2026-09-18: 0 matches in shell.js) | "Vault ranking exists in the backend; the prototype surfaces only keyword JDF search." |
| "All E2E suites are green" | verified 2026-09-17; shell, jdf ingest/search, Path B, prototype_surgical (2.2s) and prototype_surgical_live (14.1s) pass on prototype.getassureai.com; golden_path skips off the legacy host; local runs of the prototype specs still fail because the default local base URL serves the marketing page at "/". UNVERIFIED 2026-09-18: not re-run after this wave's shell/draft changes | "All six suites pass on the prototype host (or skip correctly); golden_path runs only where /app is served." |
| "A failed model call in the inquire stream is shown as a verified node" | was true until 2026-09-22: `cost_governance._default_executor` returns `"ERROR: {exc}"` with `ok=True`, and `routers/inquire_stream.py` Z3-checked that string (no metrics → PASS), persisted it via `persist_surgical_rewrite` and emitted `jdf_node_ready`. Now the text is refused before verification: SSE `error` + `complete {ok:false, persisted:false}`, nothing written (`tests/test_product_fixes.py::TestFailedModelCallNotPersisted`) | "A provider error ends the stream with an error event; no node is persisted and no truth_check is emitted." |
| "`/orchestrate` output is a live Claude/DeepSeek comparison" | with no provider keys the route serves fixed sample text (`_mock_orchestrate_payload`). Until 2026-09-22 it was labelled `status: "success"`; now `status: "mock"`, `mock: true`, `notice`, and the response is labelled so the UI can badge it (the Flask workbench's `orchestrator.js` badge was removed with the workbench on 2026-09-24) | "Without provider keys the compare panes show labelled illustrative text, not model output." |
| "`/health` `status: healthy` means every configured dependency is up" | OMP was reported `down` even when never configured, and a configured-but-down OMP left `status: healthy`. Since 2026-09-22: `omp: "not configured"` when neither `OMP_SERVER` nor `~/.omp/api_key` exists; `status: "degraded"`, `degraded: true` (HTTP 200) when OMP is configured and unreachable | "/health is healthy / degraded (optional dependency down, still 200) / unhealthy (503)." |
| "Any project id has a document" | `GET /api/projects/<id>/jdf`, `/export`, `/history` answered 200 with an empty document for ids with no `projects` row (`fetch_latest_jdf_or_empty`). Since 2026-09-22 `middleware.project_ownership_required` returns 404 `{"ok": false, "error": "Project not found."}` on GET/HEAD/DELETE for unknown ids; `default`, `sandbox`, `founder` are exempt (auto-created) and PUT/POST still create | "Reads of an unknown project are 404; the first PUT creates it." |
| "Polish only fixes grammar" | the regex pass turned `$500,000` into `$500, 000` and `2.4%` into `2. 4%`. Since 2026-09-22 punctuation between digits is left alone and `strict_preservation_check` compares numeric tokens per paragraph; a polish that changes a figure returns the original document with `preserved: false` (HTTP 422) | "Polish never returns text whose numbers differ from the input." |
```

## Added 2026-09-23 (backend audit)

| Claim the code must not make | Why | What to say instead |
|---|---|---|
| "Math Check PASS" when Z3 answered `unknown` (solver timeout) | `check()` returns `unknown` on `Z3_SOLVER_TIMEOUT_MS`; it used to be read as PASS. `ledger/truth_engine.Z3Timeout` now surfaces it | `z3_status: TIMEOUT` — "not verified", never a pass |
| `gate_status: pass` while any claim is entailment-`unsupported` | `audit_summary.compute_gate_status` now returns `review` with the count in `unverified_reason` | "review — N of M claims contradicted by their source" |
| Ingest job `skipped` after a redelivered message | `advance()` ignores stage writes on a terminal job; a redelivery answers from the row | the stored `done` result |
| Job `skipped: staged object not found` on any S3 error | `S3ObjectStore.exists` is False only on 404; other errors propagate to the retry path | the real error, and a retry |
| `/api/health` `checks.sqlite` | the probe is PostgreSQL; `checks.db` is the key (`sqlite` alias kept one release) | `checks.db` |
| "Search covers your uploaded sources" only for dock-ingested PDFs | Sources-panel uploads are now indexed in `jdf_cli_chunks` (PostgreSQL) at ingest and removed on delete | true for both paths since 2026-09-23 |
| "Textract is only a fallback, so it cannot run away" | It could: nothing metered it. `services/textract_budget` now charges each call against a monthly counter in PostgreSQL and refuses past `ASSURE_TEXTRACT_MONTHLY_USD_CAP` (default 100 USD, list price); single pages use DetectDocumentText, not AnalyzeDocument, unless `ASSURE_TEXTRACT_MODE=analyze` | "Textract spend is capped at N USD/month by the app; `/api/health` `checks.textract_budget` shows the month's pages, spend and cap" |

## Added 2026-09-25 (Parsure V1 Phase A, local models on EC2)

| Claim the code must not make | Why | What to say instead |
|---|---|---|
| "Laya is a trained triage model" | `services/laya.py` is a rule table over the quality probe's flags (`model: "rules-v1"`); nothing is fitted or learned | "Laya V1 is rule-based calibrated triage behind the router; thresholds live in quality_probe constants" |
| "Page quality detects skew, glare, noise, handwriting, tables or mixed bundles" | `quality_probe.probe_visual_quality` measures blur (Laplacian), contrast range and effective DPI only; `handwritten_image`, `table`, `mixed_bundle` appear only when the client passes a `source_kind` hint | "V1 flags `low_res`, `blurry`, `low_contrast`; the other flags are not emitted" |
| "A field's confidence is measured" when it reads `parser_default[...]` | `quality_weighted_confidence` falls back to the spec defaults (0.85 jdf-cli / 0.80 OCR-Textract / 0.50 unknown) when the parser emitted no confidence; the basis string names the default | quote the `confidence_basis` string; it says `parser_default` or `parser_confidence` |
| "Classification confidence 0.9 means 90 % likely" | `field_extractor.classify_document` is keyword counting capped at 0.9; `uncertain` below 3 hits or on a tie | "keyword-heuristic classification; override it on the report" |
| "Replay re-parsed the document" | `replay.replayed` is always `false` in V1; `replay.eligible` + `reasons` record intent (spec §7) | "replay-eligible; re-parse is deferred until durable storage" |
| "The Parsure audit log is SQLite" (spec wording) | the repo is PostgreSQL only; `parsure_audit_events` is a PostgreSQL table (schema v32) | "audit events in PostgreSQL" |
| "WebP uploads are supported" | the bundled MuPDF 1.28 has no WebP decoder (measured 2026-09-25); WebP is rejected at upload | "PDF, PNG, JPEG, TIFF, BMP and text files" |
| "A phone photo's DPI was measured" | for an image without DPI metadata the probe assumes an 11-inch long side and says so in `basis`; a 1224 px photo is therefore `low_res` | "effective DPI estimated from pixel size under the stated assumption" |
| "The compose stack uses cloud models on EC2" | `ASSURE_LLM_BACKEND=ollama` everywhere the base compose file runs; the overlays are the only place the cloud policies apply | "local Ollama on the box; cloud policies only under the staging/prod overlays" |
| "Celery tasks leak a connection per repository call" | true until 2026-09-25 (one ingest exhausted pool 5+15 and died `PoolTimeout` at `persisting`); `celery_app.ScopedTask` now runs every task body in one `db_scope()` | "one pooled connection per task" |
| "The model extracted this field" (LLM-assisted extraction) | `services/llm_extraction.py` only *offers* candidates; a value is kept solely when its quote is found verbatim in the page text and the value inside that quote — the stored value and `source_span` are the document's own characters. Grounding proves presence on the page, not that the model read the right sentence (measured 2026-09-25: 3 of 5 runs took the property-damage limit for `liability_limit`), so `llm_grounded` fields carry a 0.85 factor and land below the 0.75 auto-accept | "found on the page by a grounded model suggestion; confirm in review" (`extraction_method: llm_grounded`) |
| "Mixed bundles are detected" | boundaries come from per-page keyword classification changing between pages; nothing visual | "split where the page-level document type changes" |
| "Recall on prose documents is X %" | `qwen2.5:1.5b` is not deterministic: 72–81 % over six runs on the 68-field prose golden set (label rules alone 30.9 %) | quote the range and `tests/golden/last_run_prose.json` |
| "`/health` healthy means the models are there" | true only since 2026-09-25 and only with `ASSURE_LLM_BACKEND=ollama`: `checks.models` reads the daemon's tag list; `missing`/`unreachable` → `degraded` | "`checks.models.status` ok / missing / unreachable / not local" |
| "`docker compose up` pulls the app image" | the base file builds `assure-app:local`; worker and shell carry `pull_policy: never` because compose otherwise tried to pull the tag from Docker Hub during the build and failed (`pull access denied for assure-app`, reproduced 2026-09-25). Pulled images are the staging/prod overlays' business (`pull_policy: missing` there) | "built once locally; pulled only under an overlay" |
| "`scripts/gen-env.sh` only writes a file" | was false until 2026-09-25: a backtick-quoted `docker compose up -d` inside the unquoted heredoc was a command substitution, so generating `.env` started the stack and pulled the Ollama image (the user's "gen-env downloads ollama"). Now no backtick in the template, `tests/test_gen_env.py` runs it with stub `docker`/`curl`/… on PATH and fails if any is called | true since 2026-09-25 |
| "Changing POSTGRES_PASSWORD in .env changes the database password" | the postgres image applies it only when initialising an empty volume; a regenerated .env locked the app out on EC2 (2026-09-25). `scripts/postgres-start.sh` (the service's entrypoint) now ALTERs the role over the trust-authenticated unix socket after every start | true since 2026-09-25, on every `docker compose up` |
| "Everything runs on the box" once `ASSURE_LLM_BACKEND=bedrock` | with the Bedrock backend every model call leaves for Amazon Bedrock (Sonnet 5 drafts, Opus 5 analyses by default); documents still stay in S3/disk. `/health` `checks.models` then reads `backend: bedrock` with the two ids and `status: not local` | "models on Bedrock; documents and database on the box" |
| "qwen2.5:14b is the open-source equivalent of Claude Sonnet 5" (and the other rows of the stage table) | the stage→model table in docs/deploy-single-ec2.md is our pick for each role at a given GPU memory, not a measured equivalence; no benchmark was run (2026-09-25) | "the open-weights model we run in that role" |
| "An empty ASSURE_LLM_BACKEND means the legacy cloud policies" | since 2026-09-25 empty means: OpenRouter when OPENROUTER_API_KEY is set, otherwise the local Ollama models; the legacy OpenRouter/DeepSeek policy table is only reached with `ASSURE_LLM_BACKEND=cloud` | "empty = decided from the env; `cloud` = legacy policies" |

## Added 2026-09-26 (Parsure: classification by evidence, count units)

| Claim the code must not make | Why | What to say instead |
|---|---|---|
| "All fields not found means the OCR / ingestion failed" | in the cases examined (customer's `real_estate_policy_500697.pdf`, 2026-09-26) the page had a full text layer and the wrong **type**: keywords said `auto_claim`, so every auto-claim field was honestly empty. The orchestrator now switches to the type whose fields are actually found (`reclassify_by_evidence`) and, when nothing is found at all, says so once at document level — "No fields could be read as Auto claim." — with "document type may be wrong — change it and the fields are re-read" as the first reason. Only when the whole upload carries under 200 characters of text does the sentence add "the pages carry N characters of text; the file may be a scan the OCR could not read", with the measured count | "nothing extracted as <Type> — check the document type"; the record page's notice says which of the two it is |
| "Need attention: 3" and "62 items" are inconsistent counts | they were two units by two rules (documents by card status; fields by the queue's routing rule). Since 2026-09-26 one rule (`field_extractor.field_needs_review`) and one helper (`parsure_repository.attention_counts`) feed every surface, and every figure names its unit | "Need attention: 3 documents · 62 fields" / "62 fields across 3 documents" / "… · 62 need review" — the same 62 |
| "The model classified this document" | `classify_with_model` only relays one exact type name; the orchestrator accepts it solely when that type's fields are then found by the label pass, at confidence ≤ 0.6, and `classification.basis` reads `model suggestion (<model>), confirmed by N fields found` | "type suggested by the model and confirmed by N fields on the page" |
| "The OCR confidence of this scan was X" when `no_text` is set | for an upload with under 200 characters of text nothing was measured; `pages[].ocr_confidence` stays `null` and the card reads "The pages could not be read (N characters)." | quote the character count, not a confidence |
| "Classification confidence 0.9 is a keyword count" (blanket) | still true for `keyword heuristic` bases; an `extraction_evidence` basis is the found-field ratio (capped 0.9) and a `model_suggestion` basis the found ratio capped at 0.6 — the `basis` string says which | quote `classification.basis`; `classification.method` names the stage |
| "Deleting a source removes its data" | until 2026-09-26 the intake report stayed in every list; now `parsure_reports.deleted_at` (schema v33) hides it from cards, tables, queue, analytics and exports while its audit rows stay readable by id | "hidden from the lists; the audit trail is kept" |
| "One document, one row" after a re-upload | until 2026-09-26 a re-uploaded file listed two reports with two sets of numbers; `list_reports(current_only=True)` keeps the newest per document_id (filename when absent) | "the newest report per document; older ones reachable by id" |
| "Confidence 0.85" and "85%" are different figures | one notation now: percentages everywhere (shell already did; Parsure showed 0.xx beside it) | "85%" |
| "Assure does not address JDF nodes" (customer, 2026-09-26) | was true for intake fields until then: jdf-cli elements have no `id`, so `field_source_node_id` was `null` everywhere. Now the chunk id (`p1e0`) or the saved paragraph id (`p-…`) is on every found field, plus `tree_node_id` when a tree exists; the record page prints it and the shell jumps to the node | "every found field carries its JDF address; Sources-pane uploads carry the chunk id only (no tree)" |
| "Uploads are limited to 10 MB" | default is 25 MB since 2026-09-26 (`MAX_UPLOAD_SIZE_MB`); a 17 MB PDF was accepted and a 30 MB one refused with the limit in the message | "25 MB per file by default, set in .env" |

## Added 2026-09-26 (Export: the Verification Dossier)

| Claim the code must not make | Why | What to say instead |
|---|---|---|
| "Formal Verification Certificate" as the title of every export | a customer received it for a run with `fields_accepted 0 / fields_review 12`, a cross-document conflict and a questionable signature. The title is now `derive_trust_state`'s: "Verification Dossier — Verified / Review required / Not verified"; "Certificate" appears only in the `verified` state | the state-bound title, with the status band's counts under it |
| "No cross-run contradictions detected" / "No Red-Hat findings recorded" / "No locked claims recorded" | a check that did not run is not a check that found nothing. Cross-run comparison needs ≥ 2 runs; Red-Hat has `ran`; the intake conflicts need an intake report | "Not run — fewer than two runs recorded (1)", "Red-Hat: not run" vs "Red-Hat ran and recorded 0 findings", "0 locked claims — no claim has been locked" |
| "The PDF is rendered" when it is the text writer | `exporters/pdf_ast.py`'s fallback shipped a Helvetica text dump as the dossier when neither engine was installed (the customer's file said so on page 1). No route reaches it now: `verification_dossier.render_pdf` raises and the route answers 503 | "PDF renderer unavailable on this server" (503) with each engine's probe result; the zip bundle omits the PDF and `manifest.json` says why |
| "Signature present" when the heuristic said `questionable` | `assess_signature` "cannot confirm a handwritten signature"; the dossier prints `present but questionable — <basis>` as an unresolved item, and an unresolved signature blocks `verified` | quote the status word and the basis string |
| "The PDF and the JSON agree" by inspection | they are one dict: `build_dossier` builds `verification_state.json` and renders the HTML from it | point at `verification_state.json` in the bundle |
| "The web tier has a Playwright browser" | `playwright` the package is in the image for the e2e suite; no browser is installed there. `renderer_status()["playwright"]` reads `browser not installed: …` in the image and the dossier renders with WeasyPrint | "WeasyPrint in the image; Playwright only where a Chromium is installed (developer machines)" |

## Added 2026-09-26 (Parsure: document families, evidence states, anchors)

| Claim the code must not make | Why | What to say instead |
|---|---|---|
| "The keyword 'accident' makes this an auto document" | a CMS-1500 medical claim literally asks "Auto Accident?" (box 10b); the customer's form was typed `auto_policy` from that word plus fields every insurance form shares. `field_extractor.document_family` now names a family from strong cues (vin / dwelling / grantor / cpt …, never "accident", "claim", "premium"), and a type may be chosen only when its family agrees or none dominates | "family: medical (15 cues); the auto schema is not allowed on this page" |
| "Two found fields prove the schema" | policy number, insured name, signature, effective/expiration date are on every insurance form (`SHARED_FIELD_NAMES`); `reclassify_by_evidence` and the model confirmation now need ≥ 3 found fields with ≥ 1 type-specific one | quote `classification.evidence.type_specific_fields` |
| "Every document gets a schema" | when the cues name a family, the keyword answer is weak and no schema of that family finds 2 type-specific fields, the report is `<family>_unknown` with no taxonomy fields and `schema_mismatch: true`; forcing thirteen "not found" rows of a wrong schema was the customer's complaint | "Wrong document type — read as Medical claim?" (the family's nearest schema is `classification.suggestion`) |
| "A `<family>_unknown` or schema-mismatch report has N fields needing review" | the fields are not applicable; `field_needs_review` is false for `evidence_state: schema_mismatch`, `attention_counts` counts the document once under `schema_mismatch`, the queue shows one row | "1 document — wrong document type" |
| "`field_source_node_id: null` means the link to the JDF was lost" | an absent field now carries `evidence.kind = absent` with the searched pages, node ids and character count, and `field_source_node_id` is the anchor it was searched from (first layout node, or the tree root); `graph_integrity.orphans` is 0 whenever the parse produced node ids | "anchored to el-0 (searched)"; quote `graph_integrity` |
| "Confidence 0.00 and 'field not found' ×12 means the OCR failed" | `evidence_state` separates `not_on_document` (readable page, ≥ 200 chars and quality ≥ 0.5) from `unreadable` (< 200 chars or quality < 0.3), and the reason says which: "Not on this document type" / "Page could not be read" | quote `evidence_state` and `review_summary.evidence_states` |
| "A confidence of 0.85 × 0.28 describes this value" when the OCR read its line at 0.91 | the page score is page-global; when the segment carries a jdf-cli OCR confidence the field uses it and the basis reads `local_ocr (0.91)` instead of `page_quality (0.28)` | quote `confidence_basis`; `quality_source` says which factor was used |
| "Confidence is computed for every field" | `schema_mismatch` fields carry `extraction_confidence: null`, basis "not computed: schema mismatch" — a number there would describe a value that has no business on the page | "not computed" |
